#ifndef DSIMU_DETECTOR_TYPE_H
#define DSIMU_DETECTOR_TYPE_H

enum class TrackerStyle : int {dPlane, dStrip, dPixel};
enum class TrackerType : int {dNone, dTagging, dRecoil};

#endif // DSIMU_DETECTOR_TYPE_H
