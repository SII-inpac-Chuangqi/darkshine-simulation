---
# Begin of file
# Example by Yulei Zhang

name: "plots"
inject_hist: false

global_normalize: true

global_y_scale: 1.1
global_log_y: true
global_grid_y: true

global_ratio_plot: false
# global_ratio_samples: [ "Testing", "Reference" ]
global_ratio_range: [ 0.5 , 1.5]

global_selection: "1"
global_selection_title: "Inclusive Validation"

variables:
  - name: "ECAL_E_total[0]"
    range: [ 0, 0, 0 ] # [bin size, min, max], or set 0 for variables bin
    vbins: [6000,7000,7100,7200,7300,7400,7500,7600,7700,7800,7900,8000]
    label: [ "Truth E_{total}", "MeV" ]
    # Optional
    # selection_title: "Inclusive Validation"
    # y_scale: 0.15
    # log_y: false
    # normalize: true
    # grid_y : true
    # ratio_plot: true
    # ratio_samples: [ "Testing", "Reference" ]
    # ratio_range: [ 0.5 , 1.5]

samples:
  - name: "Results"
    # Mandatory
    file_name: "dp_ana.root"
    tree_name: "dp"
    # Optional
    # selection: "1"
    line_attr: [ 4, 5 ] # [ color, width ]
    # marker_attr: [ 2, 0.95 , 3, 1 ] #[ color, transparency, width, style ]
    fill_attr: [ 4, 0.6, 1001 ] # [ color, transparency, style ]

# End Of File
...