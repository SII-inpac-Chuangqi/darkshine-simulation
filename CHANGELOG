2022-12-05
    DSimu v1.5, DAna v1.5.5
    1. DAna: Add branch SideHCAL_E_total and SideHCAL_E_Max_Cell

2022-10-25
    DSimu v1.5, DAna v1.5.4
    1. Add Algo/ProcessReader.h . Deprecate ProcessClassifier.

2022-08-14
    1. Add DSimu arguments --seed --save_geometry --outfile_Name --Run_Number --beam_on
    2. Add yaml Biasing/BiasParticle

2022-07-25
    1. Add Side HCAL

2022-06-20
    1. Truth information stored in DTruth
    2. CICD suppot

2022-05-23
    1. DSimu: Update geometry hierarchy to speed up spatial search
    2. Related updates in DAna and DDis

2022-05-05
    1. DSimu: Add MCPHelperCollection and save_mcp_helper option, allowing save snapshot of particle passing through ECAL Region surface

2022-04-25
    1. Optimise data transmission between DAna processors

2022-04-11
    1. Add geometry reader in DAna

2022-04-10
    1. default.yaml : Added ECAL_APD_Size and HCAL_APD_Size

2022-04-06
    1. Version b1.5 of Object: Replaced double with float (storage optimization)
    2. HCAL Optical: Merged hcal_optical into master branch. Default materials in default.yaml changed. Replaced HCAL Unit 1 with WLS Unit.
    3. SteppingAction : Only record G4Step of initial electron in tracker region (storage optimization)

2022-02-19
    1. Library versioning of Utility/Object ( version b1 for baseline1 ). Processing samples with wrong DSS version is forbidden.

2021-08-11
    1. Integrated GenFit2 Library.

v2.3.0
DSimu:
    1. Add Signal (Dark Photon) Mass in yaml file
    2. Add PNZ, ENZ (Z Position), ENEnergy in DEvent
    3. Add Branch: Weight


v2.2.0
Global:
    1. Add a control class, which handle all variables, easy to manage the whole data.
    2. User can change variables by a yaml file

DSimu:
    1. Add MC Particle Contribution to Simulated Hits; Only the three most contributed particles will be recorded.
    2. Tracker Hits is pixelized along trackers z position. (hit position z is fixed for each tracker layer).
    3. Detector Overlay has been solved.
    4. new Filter is added. (See readme for more details)
    5. Dark Physics is added, still under validation.

DAna:
    1. Add geometry in config file; One can access to geomerty by gGeoManager, with including header file "TGeoManager.h"
    2. Add command "DAna -x", which will print out an example config.txt

DDis:
    1. Add the full detector geometry display.
    2. Add SimulatedHits Collection display; Add initial particle step collection display; Add MC particle tracks display.
    3. Now support Ana Processor module. One can easily visualize the processing of AnaProcessor.

DPlot:
    1. read yaml file as config file
    2. Easy to make some comparison plots

v2.1.1
Added DDis module, which can print out the detector geometry very fast

v2.1.0
Added bunches of detector messenger in DSimu, so that users can change the detector geometry in the config file, without
re-compiling the DSimu.
